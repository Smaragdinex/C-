#include <stdio.h>
#include <stdlib.h>
// 用迴圈 計算 1 + 1/2 + 1/3 + ... + 1/n 總和, n 可自行輸入 


int main(void)
{
    int i,n;
    double sum = 0;
    
    scanf("%d",&n);
    
    for(i = 1; i<=n; i++)
    {
        sum+= 1.0/i;
    }
    
    printf("%f",sum);
    
    return 0;
}
