#include <stdio.h>

double rpower(double b, int n);

int main(void)
{
    printf("%f",rpower(2.0,3));
    return 0;
}

double rpower(double b, int n)
{
    if(n==0)
        return 1.0;
    else
        return b * rpower(b,n-1);
}
    
    
