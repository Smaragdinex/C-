#include <stdio.h>
#include <stdlib.h>

int main(void)
{
    char *ptr = "We are best friends.";
    
    int count = 0;
    
    while(*ptr != '\0')
    {
        count++;
        ptr++;
    }
    printf("%d\n",count);
    
    return 0;
}

