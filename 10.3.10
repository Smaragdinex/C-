#include <stdio.h>
#include <stdlib.h>
void count(int *num);

int main(void)
{
    int num =0;
    
    count(&num);
    count(&num);
    
    printf("%d",num);
  
    return 0;
}

void count(int *num)
{
    (*num)++;
}
