#include <stdio.h>

void kitty(int k); //prototype function
int main(void)
{
    int k;
    scanf("%d",&k);
    kitty(k);

    return 0;
}

void kitty(int k)
{
    int i;
    for(i = 0; i < k; i++)
    printf("Hello Kitty\n");
}
