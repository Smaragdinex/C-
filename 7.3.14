#include <stdio.h>
//計算一條繩子長度從 3000 cm 開始，每天剪掉一半，直到長度小於 5 cm 時停止的天數計算器。

int main(void)
{
    int i, count =0, rope = 3000;
    
    while( 1 )
    {
        rope = rope /2;
        count++;
        
        if(rope < 5) break;
    }
    
    printf("%d",count);

    return 0;
}
